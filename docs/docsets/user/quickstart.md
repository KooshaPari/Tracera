# Technical User Quickstart

1. Install dependencies and start services (`task install`, `task dev:tui`).
2. Validate service health and API access.
3. Run key quality checks (`task quality`).
4. Use docs and runbooks for incident and rollout procedures.
