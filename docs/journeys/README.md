# Journey Manifests

<!-- RICH-MEDIA-STUB: type=recording-mp4 subject="TraceRTM end-to-end user journey: import requirements → trace → link → view" journey="e2e-journey-overview" status=TODO -->
> 📽️ **[RICH-MEDIA STUB — needs journey capture]** type: recording-mp4 · subject: TraceRTM end-to-end user journey: import requirements → trace → link → view · journey: `e2e-journey-overview` · status: TODO
>
> *Intended content:* a screen recording of the full TraceRTM loop — import a requirements set, generate atomic traces, link traces to artifacts, then open the matrix/coverage view. *Blocked on:* the shared `@phenotype/doc-embeds` Remotion pipeline (the `phenotype-journeys` repo is not present in this workspace) **and** a running TraceRTM stack (`make dev` brings up the Go/Python backend + frontend turbo monorepo; not booted here). Fill once the `e2e-journey-overview` manifest can be driven end-to-end.
