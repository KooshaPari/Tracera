# Specifications

- `build` should run the build command for each detected language/toolchain.
- `test` should run the default test command for each detected language/toolchain.
- `lint` should run the default lint command for each detected language/toolchain.
- `clean` should remove common build artifacts for each detected language/toolchain.
