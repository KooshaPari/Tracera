# Known Issues

- Canonical `Tracera` has unrelated local workflow, package, Docker, report,
  and test-file edits.
- `Tracera-recovered-wtrees/sladge-badge` has broken git metadata and was not
  reused.
- Worktree checkout reported existing Git LFS pointer warnings for archived
  binary/config artifacts.
