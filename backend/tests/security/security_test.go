//go:build security

package security_test

import "testing"

// TestSecurityPlaceholder is a placeholder test that passes unconditionally.
// Replace with real security tests (e.g. auth checks, injection probes).
func TestSecurityPlaceholder(t *testing.T) {
	t.Log("security package placeholder — add real tests here")
}
