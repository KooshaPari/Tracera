Rust crate workspace for Tracera backend: domain, API bindings, storage adapters, and telemetry.

Build and test using the workspace-aware cargo commands:

cargo build --workspace
cargo test --workspace
