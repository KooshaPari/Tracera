# Crates

Rust workspace crates for TraceRTM. This directory is a placeholder for future Rust components.

The project currently uses Go (`backend/`) and Python (`src/`) for server-side logic. Rust crates may be added for performance-critical services or shared libraries.
