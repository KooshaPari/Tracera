CLUSTER_START cluster=C06 repo=Tracera pillars=L51-L60 date=2026-07-05

### L51 — Pinned dependencies
score: 3  glyph: ✓
evidence:
  - Cargo.lock exists at repo root (Cargo.lock:1)
  - uv.lock exists at repo root (uv.lock:1)
  - frontend/bun.lock exists for the web workspace (frontend/bun.lock:1)
gaps:
  - No obvious lockfile verification policy surfaced in grep/ls evidence — effort: S
soft_goal_delta: locked across Rust/Python/frontend; verify step not evidenced

### L52 — Reproducible builds
score: 1  glyph: △
evidence:
  - rust-toolchain.toml pins the Rust toolchain (rust-toolchain.toml:1)
  - wrangler.toml comments reference the shim reading rust-toolchain.toml for cargo delegation (wrangler.toml:12)
gaps:
  - No evidence of SOURCE_DATE_EPOCH, bit-identical build checks, or repro CI — effort: M
soft_goal_delta: toolchain pin present; reproducibility workflow not shown

### L53 — Build provenance
score: 1  glyph: △
evidence:
  - .github/workflows/cargo-deny.yml is referenced in push_via_api.py as an active CI workflow (push_via_api.py:38, push_via_api.py:59)
  - docs/audit/AUDIT_part1.md mentions release attestation workflow coverage (.github/workflows/release-attestation.yml:40, docs/audit/AUDIT_part1.md:102-105)
gaps:
  - No direct evidence of SLSA level, GitHub OIDC, or generated provenance attestations in the checked files — effort: M
soft_goal_delta: release attestation exists; provenance standard not evidenced

### L54 — Hermetic builds
score: 0  glyph: ✗
evidence:
  - No hermetic-build control surfaced in grep/ls evidence for network-disabled builds or sandboxed fetches
  - push_via_api.py notes private git fetch behavior for cargo-deny, implying build-time network dependence (push_via_api.py:38)
gaps:
  - No evidence of no-network build enforcement in CI or release paths — effort: L
soft_goal_delta: builds still appear network-dependent

### L55 — Dependency confusion guard
score: 1  glyph: △
evidence:
  - cargo-deny is referenced in CI-related content (push_via_api.py:21, push_via_api.py:38, docs/triage/CHECK_TRIAGE.md:20)
  - Repo includes private git dependency references in CI discussion (push_via_api.py:38)
gaps:
  - No scoped private registry config, mirror, or typosquatting check evidence — effort: M
soft_goal_delta: deny tooling exists; registry-scoping guard not evidenced

### L56 — Container provenance
score: 1  glyph: △
evidence:
  - Dockerfile.local exists (Dockerfile.local:1)
  - docker-compose.yml and deploy/kubernetes manifests show containerized deployment surfaces (docker-compose.yml:1, deploy/kubernetes/templates/tracera.yaml:1)
gaps:
  - No evidence of cosign signing, SBOM attestation, or deploy-time verification — effort: L
soft_goal_delta: containers are present; provenance/signing not shown

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - crates/tracertm-mcp/Cargo.toml exists, showing one MCP crate in the workspace (crates/tracertm-mcp/Cargo.toml:1)
  - crates/tracertm-mcp/Cargo.toml and related crates are pinned only by workspace membership, not provenance metadata (crates/tracertm-mcp/Cargo.toml:1, crates/tracera-server/Cargo.toml:1)
gaps:
  - No evidence of pinned server provenance, attestation, or versioned MCP distribution — effort: M
soft_goal_delta: MCP crate exists; provenance metadata absent

### L58 — License scan
score: 1  glyph: △
evidence:
  - LICENSE exists at repo root (LICENSE:1)
  - crate manifests declare licenses (Cargo.toml:8, crates/tracertm-mcp/Cargo.toml:7, crates/tracera-server/Cargo.toml:7)
gaps:
  - No evidence of allow-list scanning, copyleft detection, or deny-on-violation CI — effort: M
soft_goal_delta: licensing is declared; scanning policy not shown

### L59 — Source code provenance
score: 0  glyph: ✗
evidence:
  - No git-signing or branch-policy evidence surfaced in grep/ls results
  - Repo docs focus on audits and workflows, not commit-signing policy (docs/audit/AUDIT_part1.md:102-105, docs/audit/AUDIT_part2.md:97-121)
gaps:
  - No evidence of commit signing per author or enforced branch protection — effort: L
soft_goal_delta: provenance chain ends at repo state, not author signatures

### L60 — Toolchain pinning
score: 2  glyph: ~
evidence:
  - rust-toolchain.toml pins Rust (rust-toolchain.toml:1)
  - uv.lock pins Python dependencies (uv.lock:1)
  - frontend/bun.lock pins the frontend dependency graph (frontend/bun.lock:1)
gaps:
  - No evidence of .python-version or SHA256 verification for toolchains — effort: M
soft_goal_delta: pinned toolchains exist; verify/SHA guards not shown

CLUSTER_TOTAL score=9/30 pct=30% grade=D
CLUSTER_DONE cluster=C06 repo=Tracera
