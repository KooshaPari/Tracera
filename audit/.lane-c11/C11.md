### L108 — Native Installer(s)
score: 2  glyph: ~
evidence:
  - frontend/apps/desktop/package.json:6-11 exposes `bun run build` and `bun run package` via Electrobun
  - frontend/apps/desktop/electrobun.config.ts:3-20 defines the app shell and macOS icon bundle for a native desktop package
  - frontend/apps/desktop/README.md:34-42 documents release packaging and says artifacts land in `frontend/apps/desktop/build/`
gaps:
  - No verified clean-build artifact evidence for all claimed desktop OS targets — effort: M
soft_goal_delta: package exists; cross-OS installer verification still missing

### L109 — CLI Packaging & Install Channels
score: 1  glyph: △
evidence:
  - bin/tracera:1-20 and bin/tracera-attach:1-12 provide checked-in launch wrappers instead of install-channel packaging
  - Cargo.toml and crates/tracertm-mcp/Cargo.toml expose Rust binaries, but no publish/install channel is documented in the checked-in docs
  - frontend/package.json:1-18 and frontend/apps/desktop/package.json:1-19 cover frontend/desktop scripts, not standard CLI distribution
  - docs/01-getting-started/CLI_TUTORIAL.md:1-80 covers local use but not a pinned installer one-liner
gaps:
  - No documented `cargo install`/`brew install`/`npm i -g`/`uv tool install` channel for a primary CLI — effort: M
soft_goal_delta: binaries exist; ecosystem install path not yet productized

### L110 — Tray / Menubar Client
score: 3  glyph: ✓
evidence:
  - frontend/apps/desktop/README.md:65-70 describes a system tray app with Show/Reload/Quit and tray-resident behavior
  - frontend/apps/desktop/src/index.ts:120-220 wires tray menu actions and keeps the window alive from the tray
  - frontend/apps/desktop/electrobun.config.ts:15-20 references desktop packaging assets used by the tray client
  - frontend/apps/desktop/tests/e2e_desktop.test.ts:1-120 exercises desktop launch behavior around the tray shell
gaps:
  - None material from grep/ls evidence; tray UX is present and wired — effort: S
soft_goal_delta: tray/menubar surface is implemented and documented

### L111 — Auto-Update
score: 0  glyph: ✗
evidence:
  - rg for auto-update/self-update/update-channel terms found no updater config in the checked-in desktop or CLI manifests
  - frontend/apps/desktop/package.json:6-11 only defines build/package/test scripts
  - no release notes or updater workflow references were surfaced in the grep/ls pass for a signed update channel
gaps:
  - No auto-updater, self-update command, or documented update channel — effort: M
soft_goal_delta: not started

### L112 — Code-Signing & Notarization
score: 0  glyph: ✗
evidence:
  - rg for codesign/notariz/sigstore/cosign returned no signing workflow or packaging config in the desktop release path
  - frontend/apps/desktop/electrobun.config.ts:3-20 defines the package but not signing or notarization settings
  - .github/workflows/release-desktop.yml and .github/workflows/release-crates.yml exist, but the grep/ls pass did not surface signing steps
gaps:
  - No signing/notarization pipeline for desktop artifacts — effort: M
soft_goal_delta: not started

### L113 — Deploy Surface: Container / OCI
score: 2  glyph: ~
evidence:
  - docker-compose.yml:1-22 defines a containerized stack entrypoint
  - deploy/selfhost/docker-compose.selfhost.yml:1-24 provides a self-hosted compose path
  - deploy/selfhost/README.md:1-80 documents local self-host deployment
gaps:
  - No OCI image build/publish workflow surfaced in grep/ls evidence — effort: M
soft_goal_delta: container deployment is present; registry delivery not confirmed

### L114 — Deploy Surface: Serverless / Edge (Vercel / Workers)
score: 2  glyph: ~
evidence:
  - wrangler.toml:1-34 configures Workers deployment with observability and KV/R2 bindings
  - .github/workflows/deploy-vercel.yml exists, indicating a Vercel deploy surface
  - frontend/apps/docs/.vercel/project.json and .vercel README markers show Vercel linkage for the docs app
gaps:
  - Edge/serverless deployment exists, but the full prod/staging contract was not validated from grep/ls alone — effort: M
soft_goal_delta: edge deploy surfaces are configured

### L115 — Deploy Surface: Traditional Server (Caddy / systemd / Reverse-Proxy)
score: 3  glyph: ✓
evidence:
  - deploy/selfhost/Caddyfile:1-22 provides reverse-proxy configuration for self-hosted serving
  - .deploy/launch-tracera.sh:1-18 and .deploy/launch-tracera.command:1-16 provide local launch wrappers for a traditional server-style runtime
  - deploy/selfhost/docker-compose.selfhost.yml:1-24 pairs with the reverse proxy for server deployment
gaps:
  - No major gap surfaced from grep/ls evidence; traditional-server path is explicit — effort: S
soft_goal_delta: reverse-proxy/server deployment is clearly supported

### L116 — Deploy-Surface Parity & Robustness
score: 2  glyph: ~
evidence:
  - .github/workflows/e2e.yml:25-26 builds tracera-server and curls it in CI
  - .github/workflows/nightly.yml:1-40 exists as a second runtime validation surface
  - deploy/selfhost/README.md:1-80 and wrangler.toml:1-34 show multiple supported delivery modes
gaps:
  - No single parity matrix or deploy contract that proves all supported surfaces behave equivalently — effort: M
soft_goal_delta: multiple deploy modes exist; parity is not codified

### L117 — Mobile Presence Decision
score: 0  glyph: ✗
evidence:
  - No mobile app source, build target, or mobile deployment config surfaced in grep/ls evidence
  - The visible app surfaces are desktop, web, server, container, and edge
  - No mobile-specific docs or workflows appeared in the repo scan
gaps:
  - No explicit mobile strategy or decision record — effort: S
soft_goal_delta: not started

### L118 — Release Automation
score: 2  glyph: ~
evidence:
  - .github/workflows/release-desktop.yml:1-40 exists for desktop release automation
  - .github/workflows/release-crates.yml:1-40 exists for crate release automation
  - .github/workflows/release-dist.yml:1-40 exists for distribution packaging automation
  - push_via_api.py:37-68 records prior release workflow fixes and release-plz handling
gaps:
  - No end-to-end release checklist or artifact promotion proof surfaced from grep/ls — effort: M
soft_goal_delta: release workflows exist; promotion flow not fully evidenced

### L119 — Versioning & Compatibility Policy
score: 1  glyph: △
evidence:
  - Cargo.toml and frontend/apps/desktop/package.json both carry version fields at 0.1.0
  - frontend/apps/desktop/README.md:34-42 frames release packaging but does not define compatibility policy
  - .github/workflows/release-crates.yml and release-dist.yml indicate versioned release intent
gaps:
  - No documented compatibility or migration policy for client/server/package versions — effort: S
soft_goal_delta: version fields exist; compatibility policy is implicit only

### L120 — Distribution Channels & Discoverability
score: 2  glyph: ~
evidence:
  - docs/01-getting-started/README.md and docs/01-getting-started/CLI_TUTORIAL.md provide user-facing entry docs
  - frontend/apps/docs/.vercel/project.json and .github/workflows/deploy-vercel.yml indicate a public web distribution path
  - frontend/apps/desktop/README.md:3-15 presents the desktop shell and production URL target
gaps:
  - No explicit app-store/package-registry discoverability plan surfaced in grep/ls evidence — effort: M
soft_goal_delta: web and desktop distribution exist; marketplace discoverability is absent

### L121 — Install/Uninstall Cleanliness & QOL
score: 2  glyph: ~
evidence:
  - frontend/apps/desktop/README.md:67-70 documents tray-resident behavior and close-to-tray UX
  - frontend/apps/desktop/scripts/postinstall.ts exists, implying install-time setup logic
  - .deploy/launch-tracera.sh and .deploy/launch-tracera.command provide lightweight launch wrappers for local setups
gaps:
  - No uninstall cleanup, launch-at-login toggle, or artifact-removal flow surfaced from grep/ls — effort: M
soft_goal_delta: install-time QOL exists; teardown hygiene is not documented

### L122 — Deploy Observability & Health Endpoints
score: 3  glyph: ✓
evidence:
  - deploy/kubernetes/templates/tracera.yaml:34-67 wires RUST_LOG, TRACE_LOG_LEVEL, TRACERA_BACKEND_URL, and OTEL_EXPORTER_OTLP_ENDPOINT into the deployment
  - deploy/kubernetes/templates/tracera.yaml:58-67 defines /health and /health/ready liveness/readiness probes
  - wrangler.toml:32-34 enables observability for the Workers surface
  - .github/workflows/e2e.yml:25-26 exercises a built server in CI as a runtime health check
gaps:
  - None material from grep/ls evidence; health/observability hooks are explicitly present — effort: S
soft_goal_delta: deploy health and observability are wired

CLUSTER_TOTAL score=18/36 pct=50% grade=C
CLUSTER_DONE cluster=C11 repo=Tracera
