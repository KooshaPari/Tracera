CLUSTER_START cluster=C03 repo=tracera pillars=L30 date=2026-07-04

### L30.1 — Spec & FR Clarity
score: 0  glyph: ✗
evidence:
  - MISSING: FUNCTIONAL_REQUIREMENTS.md / docs/functional_requirements.md (grep returned 0 in repo root)
  - MISSING: llms.txt at repo root (no agent-condensed context file)
  - README.md exists at repo root but is install/run oriented, not a spec/FR catalog
gaps:
  - No machine-readable FR-NNN catalog → agents cannot decompose tasks against stable IDs — effort: M
  - Add llms.txt sibling of README.md — effort: S
soft_goal_delta: not started

### L30.2 — Task Decomposability
score: 2  glyph: ~
evidence:
  - Cargo workspace with 4+ crates (src/, crates/, tests/) plus frontend/apps/desktop monorepo — naturally decomposable
  - README.md declares build/test commands at repo root
  - MISSING: PLAN.md / WORK_DAG.md / docs/tasks/ (no in-repo effort-tagged DAG)
  - 24 issues open on GitHub (tracera-org visible via release-desktop.yml references) but no in-repo mirror
gaps:
  - No in-repo effort-tagged WBS that names FR-IDs as DAG nodes — effort: M
soft_goal_delta: crate boundaries support decomposition; DAG wiring missing

### L30.3 — Test-as-Guardrail Coverage
score: 2  glyph: ~
evidence:
  - 95 files carry `#[test]` or `#[tokio::test]` (grep over crates/ + src/)
  - .github/workflows/e2e.yml + nightly.yml present
  - tests/ directory with multiple integration tests
  - MISSING: cargo-llvm-cov / tarpaulin / coverage threshold gating (no committed coverage config)
gaps:
  - No coverage gate enforces ≥60% — coverage could silently regress — effort: S
soft_goal_delta: tests present; coverage not gated

### L30.4 — Docs-as-Agent-Context
score: 1  glyph: △
evidence:
  - README.md present at repo root (Quick Start / build / run)
  - MISSING: docs/architecture/ tree (no REPOSITORY_MAP.md / ARCHITECTURE.md)
  - MISSING: llms.txt (L30.1 cross-cut)
gaps:
  - Add docs/architecture/ tree with REPO_MAP + ARCHITECTURE — effort: S
soft_goal_delta: README only; no deep doc tree

### L30.5 — Build & Setup Hermetics
score: 2  glyph: ~
evidence:
  - rust-toolchain.toml present (toolchain pinned)
  - uv.lock present (Python deps pinned via uv)
  - Cargo.lock present
  - .env.example MISSING (no env template)
gaps:
  - Add .env.example + document required env (RUST_LOG / TRACE_LOG_LEVEL / OTEL_EXPORTER_OTLP_ENDPOINT per Chart.yaml) — effort: S
soft_goal_delta: toolchain + Python pinned; env not templated

### L30.6 — User-Story Gap Detection Readiness
score: 0  glyph: ✗
evidence:
  - MISSING: USER_JOURNEYS.md / docs/user_journeys/ (grep returned 0)
  - No FR-coverage report / journey-traceability tooling wired
gaps:
  - No user-journey definitions or outside-in journey tests — effort: M
soft_goal_delta: not started

### L30.7 — Visual / Creative Polish Gap Detection Readiness
score: 0  glyph: ✗
evidence:
  - Tracera has a desktop app (frontend/apps/desktop) but no VISUAL_SPEC.md
  - No tests/golden/ directory; no visual snapshot tests
  - No theme/tokens file (see C10 L96.1 for the upstream gap)
gaps:
  - Add VISUAL_SPEC.md + tests/golden/ for desktop UI — effort: M
soft_goal_delta: not started

### L30.8 — Autonomous PR Quality
score: 0  glyph: ✗
evidence:
  - MISSING: .github/PULL_REQUEST_TEMPLATE.md (no What/Why/How-tested/FR-IDs/Risk/Rollback template)
  - .github/workflows/{e2e,nightly,security-guard,codeql}.yml provide CI gates but no template enforces FR-IDs
gaps:
  - Add PR template enforcing 6 required sections + FR-ID linking — effort: S
soft_goal_delta: CI gates present; template missing

### L30.9 — Conflict & Concurrency Safety
score: 1  glyph: △
evidence:
  - crates/ workspace isolation supports parallel work
  - No claim/lease tooling visible; multi-agent coordination relies on git worktree discipline (CLAUDE.md absent)
gaps:
  - Add AGENTS.md "Worktree/Parallel agents" section — effort: S
soft_goal_delta: workspace isolated; no documented parallel-agent protocol

### L30.10 — Feedback Loop Speed
score: 2  glyph: ~
evidence:
  - Cargo workspace enables scoped `cargo test -p <crate>` fast loops
  - uv workspace (uv.lock) enables fast Python dep install
  - MISSING: hyperfine-measured check/test timing committed; no sccache/nextest config
gaps:
  - Add hyperfine baseline + nextest/sccache for faster CI — effort: S
soft_goal_delta: scoped crates; no measured budget

### L30.11 — Agent Context Entrypoint
score: 0  glyph: ✗
evidence:
  - MISSING: AGENTS.md at repo root (ls AGENTS.md → absent)
  - MISSING: CLAUDE.md at repo root (ls CLAUDE.md → absent)
  - No build/test/lint/forbidden-ops entrypoint for AI agents
gaps:
  - Add AGENTS.md + CLAUDE.md with working-dir/build/test/lint/key-files/forbidden-ops/gotchas — effort: S
soft_goal_delta: not started

### L30.12 — Story-Gap & Friction Detection Tooling
score: 0  glyph: ✗
evidence:
  - MISSING: docs/friction-log.md AND .github/ISSUE_TEMPLATE/user-friction.yml
  - No FR-coverage/traceability tooling wired (no integration with AgilePlus/tracera in CI)
gaps:
  - No friction-log / user-friction intake / FR-gap report — effort: M
soft_goal_delta: not started

CLUSTER_TOTAL score=10/36 pct=28% grade=F
CLUSTER_DONE cluster=C03 repo=tracera