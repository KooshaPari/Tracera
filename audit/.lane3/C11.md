CLUSTER_START cluster=C11 repo=tracera pillars=L108-L122 date=2026-07-04

### L108.1 — Native Installer(s)
score: 1  glyph: △
evidence:
  - .github/workflows/release-desktop.yml builds matrix across macos-latest, windows-latest, ubuntu-latest
  - frontend/apps/desktop/package.json: `bunx electrobun build` (org-standard desktop framework)
  - Workflow uploads `*.dmg`, `*.zip`, `*.exe`, `*.AppImage`, `*.deb` artifacts
  - MISSING: actual artifact verification (no evidence of `Contents/Info.plist` or signature; clean-build evidence absent — see [[feedback_electrobun_codesign_noop_build]])
gaps:
  - Verify release-desktop.yml produces a real .app bundle in a clean checkout (no-op trap) — effort: M
soft_goal_delta: matrix present; artifact shape unverified

### L109.1 — CLI Packaging & Install Channels
score: 1  glyph: △
evidence:
  - .github/workflows/release-crates.yml publishes to crates.io (Cargo.toml workspace with [[bin]] likely)
  - MISSING: brew install / cargo install / uv tool install one-liner in README.md (grep → 0)
  - MISSING: shell-completions; no --version assertion in CI
gaps:
  - Add install one-liner in README (cargo install tracera-server / brew tap) — effort: S
soft_goal_delta: crates.io publish path; no consumer-facing install one-liner

### L110.1 — Tray / Menubar Client
score: 0  glyph: ✗
evidence:
  - No tray code in frontend/apps/desktop/src (grep `tray|menubar|SystemTray|NSStatusItem` outside node_modules → 0)
  - The electrobun framework supports tray (node_modules has `createTray` FFI), but Tracera does not use it
  - Tracera is a desktop app, not a daemon — tray partial NOT_APPLICABLE, but background-tray for live ingest status fits
gaps:
  - Add tray/menubar client for live ingest status (matches operator "tray app" QOL ask) — effort: M
soft_goal_delta: not started

### L111.1 — Auto-Update
score: 0  glyph: ✗
evidence:
  - MISSING: updater config in frontend/apps/desktop (grep `updater|self.update|Sparkle|appcast` outside node_modules → 0)
  - release-desktop.yml has no auto-update artifact publishing step
  - Users cannot self-update once installed
gaps:
  - Add electrobun-updater or self-update binary; publish update manifest in release pipeline — effort: M
soft_goal_delta: not started

### L112.1 — Code-Signing & Notarization
score: 0  glyph: ✗
evidence:
  - release-desktop.yml explicitly: `CSC_IDENTITY_AUTO_DISCOVERY: false` comment — "No code signing configured — produces unsigned binaries (still installable on personal machines)"
  - MISSING: codesign/notarytool/signtool/Authenticode in any workflow (grep → 0)
  - MISSING: signing secrets (APPLE_ID / CERT_*) referenced in CI
gaps:
  - Add Developer-ID signing + notarization + stapling for macOS + Authenticode for Windows — effort: L
soft_goal_delta: not started (explicitly disabled in CI)

### L113.1 — Deploy Surface: Container / OCI
score: 2  glyph: ~
evidence:
  - .container-runtime-context/Dockerfile present (debian:bookworm-slim, tracera user, port 8080)
  - Dockerfile.local present (dev variant)
  - docker-compose.yml present at repo root
  - MISSING: Apple `container` / Podman manifests (org standard is OCI-not-Docker)
  - MISSING: distroless / scratch base for prod
gaps:
  - Migrate to Apple `container` org-standard + add Podman quadlet — effort: M
soft_goal_delta: Docker present; OCI-native not adopted

### L114.1 — Distribution Surface: PyPI / crates.io
score: 2  glyph: ~
evidence:
  - release-crates.yml publishes to crates.io
  - uv.lock + pyproject.toml present (Python SDK on PyPI implied)
  - MISSING: explicit PyPI publish workflow (.github/workflows/release-pypi.yml absent)
gaps:
  - Add release-pypi.yml + confirm PyPI token wiring — effort: S
soft_goal_delta: crates.io wired; PyPI publish path unclear

### L115.1 — Helm Chart / K8s Manifests
score: 2  glyph: ~
evidence:
  - deploy/kubernetes/Chart.yaml present (Helm chart)
  - References OTEL_EXPORTER_OTLP_ENDPOINT / RUST_LOG / TRACERA_BACKEND_URL via ConfigMap
  - MISSING: helm-docs / chart-tests in CI
gaps:
  - Add helm-unittest + chart README generated — effort: S
soft_goal_delta: chart present; CI lint absent

### L116.1 — Surface Parity (Web / Desktop / CLI / SDK)
score: 1  glyph: △
evidence:
  - Three surfaces present: web (frontend/), desktop (frontend/apps/desktop), CLI/server (src/, crates/)
  - Python SDK in pyproject.toml
  - MISSING: surface parity matrix / docs (no SURFACES.md listing feature coverage per surface)
gaps:
  - Add SURFACES.md mapping features ↔ surface coverage — effort: S
soft_goal_delta: 3 surfaces shipped; parity not documented

### L117.1 — Mobile App
score: 0  glyph: ✗
evidence:
  - No mobile app — frontend/ has only desktop + web
  - MISSING: iOS / Android client (operator's explicit "mobile app" QOL ask)
gaps:
  - Add mobile client (or document NOT_APPLICABLE with reason if desktop-only is the deliberate choice) — effort: XL
soft_goal_delta: not started

### L118.1 — Update Channel Stability
score: 0  glyph: ✗
evidence:
  - No update channel / no staged rollout (auto-update absent — see L111.1)
  - No semver discipline documented
gaps:
  - Define stable/beta channels + staged rollout policy — effort: M
soft_goal_delta: not started

### L119.1 — Release Process Automation
score: 2  glyph: ~
evidence:
  - .github/workflows/release-{desktop,crates,dist}.yml + deploy-{pages,vercel}.yml present
  - release-desktop.yml triggers on `v*` tag push
  - MISSING: release-plz / cargo-release / release-please automation (manual tag)
gaps:
  - Adopt release-please for crates + desktop — effort: M
soft_goal_delta: 3 release workflows; manual tag push

### L120.1 — SLSA / Provenance Attestation
score: 1  glyph: △
evidence:
  - No SLSA provenance / sigstore / in-toto attestations
  - release-crates.yml may have provenance hooks (not audited in detail)
gaps:
  - Add SLSA L3 provenance + sigstore signing for release artifacts — effort: L
soft_goal_delta: not started

### L121.1 — Dependency Pinning & Reproducibility
score: 3  glyph: ✓
evidence:
  - Cargo.lock pinned (workspace)
  - uv.lock pinned (Python)
  - rust-toolchain.toml pinned
  - GitHub Action versions pinned (uses actions/checkout@v4 — verified in workflows)
gaps:
  - Upgrade actions/checkout to v6 (Org standard per 2026-05-05 audit) — effort: S
soft_goal_delta: full lock + toolchain pin; one action version behind

### L122.1 — Rollback & Disaster Recovery
score: 0  glyph: ✗
evidence:
  - No rollback runbook / docs/RUNBOOK.md / docs/INCIDENT.md
  - Helm chart likely supports rollback via helm rollback, but no documented policy
  - No backup/restore tooling
gaps:
  - Add docs/RUNBOOK.md + helm-rollback policy + SQLite backup procedure — effort: M
soft_goal_delta: not started

CLUSTER_TOTAL score=14/45 pct=31% grade=F
CLUSTER_DONE cluster=C11 repo=tracera