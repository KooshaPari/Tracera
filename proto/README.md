TraceRTM gRPC Protocol Buffers (v1): core, agents, integrations, common.

Use `buf generate` (preferred) or `protoc` to generate language bindings.

Generate for Go/Python from repository root as documented in project scripts.
